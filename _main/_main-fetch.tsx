// import {parseJSON, checkStatus} from './_main-utils';

// export default function callFetch(url: string | URL | Request, options = {}) {
//   return fetch(url, options).then(checkStatus).then(parseJSON);
// }
// export default async function callFetch(url: string | URL | Request, options = {}) {
//   const response = await fetch(url, options);
//   const response_1 = await checkStatus(response);
//   return parseJSON(response_1);
// }
